#include <iostream>
#include <fstream>

#include "headers/Analyzer.h"

void Analyzer::sens_analysis(std::map<std::string, std::vector<int>> times) {
    std::ofstream file("Results.csv");

    // perfrom sensitivity analysis on all the algorithms

    file.close();
}
